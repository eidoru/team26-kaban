{
  "name": "kaban",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run db:generate && npm run build -w client",
    "postinstall": "npm run db:generate",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "test": "npm run test -w server",
    "typecheck": "npm run build -w server && npm run build -w client",
    "dev:local": "dotenv -e .env.localdb -- npm run dev",
    "db:local:start": "supabase start && node scripts/local-env.mjs && npm run db:local:migrate",
    "db:local:stop": "supabase stop",
    "db:local:migrate": "dotenv -e .env.localdb -- prisma migrate deploy",
    "db:local:reset": "supabase db reset && npm run db:local:migrate"
  },
  "devDependencies": {
    "@vercel/node": "^5.3.0",
    "concurrently": "^9.1.2",
    "dotenv-cli": "^11.0.0",
    "prisma": "^6.8.2",
    "supabase": "^2.117.0",
    "typescript": "^5.8.3"
  },
  "allowScripts": {
    "bcrypt@5.1.1": true
  }
}
